#include <bits/stdc++.h>

using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  int n, k, q;
  cin >> n >> k >> q;
  int d[n];
  for (int i = 0; i < n; i++) {
    cin >> d[i];
  }
  return 0;
}
